<UserControl x:Class="Evently.Client.Wpf.Features.Cart.CartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Evently.Client.Wpf.Features.Cart"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button Content="← Return" HorizontalAlignment="Left" Margin="0,0,0,16"
                Padding="12,6" Background="Transparent" Foreground="#4A90D9"
                BorderThickness="0" Cursor="Hand" Click="BackButton_Click" />

        <TextBlock Grid.Row="1" Text="Cart" FontSize="28" FontWeight="Bold"
                   Foreground="#1A1A2E" Margin="0,0,0,16" />

        <TextBlock Grid.Row="2" Text="Loading..."
                   FontSize="16" Visibility="{Binding Model.IsLoading, Converter={StaticResource BoolToVisibility}}"
                   HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Border Grid.Row="2" Background="#FFF0F0" CornerRadius="8" Padding="16"
                Visibility="{Binding Model.Error, Converter={StaticResource NullToCollapsed}}" VerticalAlignment="Top">
            <TextBlock Text="{Binding Model.Error}" Foreground="#D32F2F" FontSize="14" />
        </Border>
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Model.Items}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="#F5F5F5" CornerRadius="8" Padding="16" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="{Binding TicketTypeName}" FontSize="16"
                                           FontWeight="SemiBold" />
                                <TextBlock Foreground="#666" FontSize="13" Margin="0,4,0,0">
                                    <Run Text="Quantity: " />
                                </TextBlock>
                            </StackPanel>
                            <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="16,0">
                                <Run Text="{Binding UnitPrice, StringFormat='{}{0:F2}'}" FontSize="16"
                                     FontWeight="Bold" Foreground="#4A90D9" />
                                <Run Text="{Binding Currency}" FontSize="13" Foreground="#666" />
                            </TextBlock>
                            <Button Grid.Column="2" Content="Delete" Padding="12,6"
                                    Background="#FF5252"
                                    Foreground="White" BorderThickness="0" Cursor="Hand" Tag="{Binding TicketTypeId}"
                                    Click="RemoveItem_Click">
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Border Grid.Row="3" Background="White" Padding="16" CornerRadius="8" Margin="0,8,0, 0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Total: " FontSize="20" VerticalAlignment="Center" />
                <TextBlock Text="{Binding Model.TotalPrice, StringFormat='{}{0:F2} PLN'}" FontSize="24"
                           FontWeight="Bold" Foreground="#4A90D9" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>