<UserControl x:Class="Evently.Client.Wpf.Features.Events.EventListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0"
                 Text="{Binding Model.SearchTerm, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" Padding="8"
                 FontSize="14" />

        <TextBlock Grid.Row="1" Text="Loading..." FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding Model.IsLoading, Converter={StaticResource BoolToVisibility}}" />

        <TextBlock Grid.Row="1" Text="{Binding Model.Error}" Foreground="Red" HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="{Binding Model.Error, Converter={StaticResource NullToCollapsed}}" />
        <ListBox Grid.Row="1" ItemsSource="{Binding Model.Events}" Background="Transparent" BorderThickness="0" MouseDoubleClick="EventList_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" CornerRadius="6" Margin="0,0,0,8" Padding="16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Location}" Foreground="Gray" Margin="0,4,0,0" />
                                <TextBlock Text="{Binding StartsAtUtc, StringFormat='{}{0:dd MMM yyyy}'}"
                                           Foreground="Gray" />
                            </StackPanel>
                            <Border Grid.Column="1" Background="#E8F5E9" CornerRadius="4" Padding="8,4"
                                    VerticalAlignment="Top">
                                <TextBlock Text="{Binding Status}" Foreground="#388E3C" />
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
            <Button Content="←" Width="36" Command="{Binding PrevPageCommand}" />
            <TextBlock VerticalAlignment="Center" Margin="12,0" Text="{Binding Model.Page, StringFormat='Page {0}'}" />
            <Button Content="→" Width="36"
                    Command="{Binding NextPageCommand}" />
        </StackPanel>
    </Grid>
</UserControl>